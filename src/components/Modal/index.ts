export {
  Modal,
  ModalContent,
  ModalHeader,
  ModalBody,
  ModalFooter,
} from './Modal';

export { useModal } from './useModal';

export type {
  ModalProps,
  ModalContentProps,
  ModalHeaderProps,
  ModalBodyProps,
  ModalFooterProps,
  ModalSize,
  ModalRadius,
  ModalShadow,
  ModalBackdrop,
  ModalScrollBehavior,
  ModalPlacement,
  ModalSlots,
  ModalContextType,
  UseModalProps,
  UseModalReturn,
} from './types';

export { modal } from './variants';
export type { ModalVariantProps } from './variants';